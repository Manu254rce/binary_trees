# Makefile for the binary trees task

CC = gcc
SRC = $(wildcard *.c) 
NAME = binary_trees
RM = rm -f
CFLAGS = -Wall -Werror -Wextra -pedantic -std=gnu89 -Wunused-variable

all:
	$(CC) $(CFLAGS) $(SRC) -o $(NAME)

lint:
	betty $(SRC)

clean:
	$(RM) *~ *.*~ .*.*~ .*.*.un~ $(NAME) 
